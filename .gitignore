node_modules/
dist/
build/
data/
*.log
.env
.env.local
.DS_Store
web/dist/
server/dist/
server/public/
coverage/
*.tsbuildinfo
# Desktop (Electron) build artifacts
desktop/.gen/
desktop/release/
# Local agent runbooks/skills: may contain server creds; never commit.
.claude/skills/
report.md
# Playwright: run artefacts and the saved e2e session cookie. The session file
# is a live owner credential for whatever BASE_URL it was captured against.
test-results/
playwright-report/
e2e/.auth/
